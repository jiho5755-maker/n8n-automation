#!/bin/bash
# ──────────────────────────────────────────────────────────────
# 강사 신청 폼 서버 배포 스크립트
# 사용법: ./deploy.sh
# ──────────────────────────────────────────────────────────────

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
KEY="$HOME/.ssh/oracle-n8n.key"
SERVER="ubuntu@158.180.77.201"
REMOTE_PATH="/var/www/pressco21/forms/instructor-apply.html"

echo "📤 배포 시작..."
scp -i "$KEY" "$SCRIPT_DIR/instructor-apply.html" "$SERVER:$REMOTE_PATH"

if [ $? -eq 0 ]; then
  echo "✅ 배포 완료: https://nocodb.pressco21.com/apply"
else
  echo "❌ 배포 실패. SSH 키 또는 네트워크 상태를 확인하세요."
  exit 1
fi
