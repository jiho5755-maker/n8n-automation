# ============================================
# n8n 서버 환경 변수 (.env)
# 이 파일을 .env로 복사해서 값을 채워 넣으세요
# cp .env.example .env
# ============================================

# 도메인 (서브도메인 권장, 예: n8n.yourdomain.com)
DOMAIN_NAME=n8n.yourdomain.com

# PostgreSQL 비밀번호 (강력한 비밀번호 사용)
# 생성 명령: head /dev/urandom | tr -dc A-Za-z0-9_ | head -c 24 ; echo
POSTGRES_PASSWORD=여기에_강력한_비밀번호_입력

# n8n 암호화 키 (credential 암호화용, 절대 변경 금지)
# 생성 명령: head /dev/urandom | tr -dc A-Za-z0-9_ | head -c 32 ; echo
N8N_ENCRYPTION_KEY=여기에_32자_암호화키_입력
